# 알고리즘 코딩테스트 마스터 로드맵

이 문서는 문제 수를 채우는 계획이 아니라, 처음 보는 문제를 제한 시간 안에
분류하고 증명하고 구현하는 능력을 만드는 운영 매뉴얼이다. 이 저장소의 57문제는
기초 재료이고, 목표는 다음 네 능력을 동시에 갖추는 것이다.

1. 제약조건만 보고 허용 시간복잡도를 계산한다.
2. 문제의 상태와 연산을 자료구조·그래프로 정확히 모델링한다.
3. 풀이가 맞는 이유를 불변식이나 귀납으로 설명한 뒤 구현한다.
4. 반례를 스스로 만들어 제한 시간 안에 오류를 제거한다.

## 1. 마스터의 사고 순서

문제를 읽자마자 코드를 쓰지 않는다. 아래 순서를 종이에 적지 않고도 반복할 수
있을 때까지 훈련한다.

### 1단계: 출력 한 문장으로 다시 쓰기

“무엇을 반환하는가?”를 수학적으로 한 문장에 적는다. 최솟값인지, 경우의 수인지,
존재 여부인지, 처리 후 상태인지가 먼저 확정되어야 한다.

### 2단계: 입력 크기로 복잡도 상한 계산

Python에서 대략적인 출발점은 다음과 같다. 상수와 실제 연산 비용에 따라 달라지므로
절대 법칙이 아니라 후보 제거용 기준으로 쓴다.

| 최대 입력 크기 | 우선 검토할 복잡도 |
|---:|---|
| 8~10 | 순열·부분집합 `O(n!)`, `O(2^n)` 가능 |
| 20 | `O(2^n)`을 경계로 검토 |
| 100~500 | `O(n^3)` 가능성 검토 |
| 1,000~5,000 | `O(n^2)` 가능성 검토 |
| 100,000 | `O(n log n)` 또는 `O(n)` 목표 |
| 1,000,000 | 거의 항상 `O(n)` 또는 작은 상수의 `O(n log n)` |
| 10억 이상의 답 범위 | 값 자체 순회 대신 이분탐색·수학적 계산 검토 |

### 3단계: 상태, 선택, 전이를 분리

- 상태: 지금까지 무엇을 알면 미래 결정을 내릴 수 있는가?
- 선택: 이 시점에 가능한 행동은 무엇인가?
- 전이: 선택 후 상태가 어떻게 바뀌는가?
- 목표: 어느 상태에서 답을 갱신하거나 탐색을 끝내는가?

이 네 질문은 시뮬레이션, 그래프, DP, 그리디에 공통으로 적용된다.

### 4단계: 가장 단순한 정답부터 만든다

입력이 작다면 완전탐색을 기준 정답으로 삼는다. 큰 입력이라면 먼저 느린 풀이를
정의하고, 반복 계산이나 불필요한 후보가 무엇인지 찾아 최적화한다. 최적 풀이를
처음부터 떠올리려는 습관보다 “정답 모델 → 병목 제거”가 재현성이 높다.

### 5단계: 핵심 불변식을 말한다

코딩 전 다음 형태의 문장을 만들 수 있어야 한다.

- “큐에 들어간 정점은 아직 처리되지 않았지만 최단 거리는 확정되었다.”
- “스택은 현재까지 제거되지 않은 후보만 단조 순서로 보관한다.”
- “`dp[i]`는 i번째까지 처리했을 때 가능한 최적값이다.”
- “`check(x)`가 참이면 x보다 완화된 모든 값도 참이다.”

불변식을 말하지 못하면 구현 중 조건문을 임의로 추가할 가능성이 높다.

### 6단계: 반례를 먼저 설계한다

최소 입력, 최대 입력, 전부 같은 값, 이미 정렬/역정렬, 답이 없음, 답이 여러 개,
경계에 답이 있음, 중복, 연결되지 않음, 한 줄/한 칸 격자를 우선 확인한다.

## 2. 한 문제를 실력으로 바꾸는 학습 루프

### A. 최초 풀이: 40분 제한

1. 5분: 입출력과 제약을 요약한다.
2. 10분: 브루트포스와 목표 복잡도를 적는다.
3. 10분: 자료구조, 불변식, 반례를 적는다.
4. 15분: 구현한다.

40분 뒤에도 핵심 아이디어가 없으면 힌트를 한 단계만 본다. 정답 코드를 곧바로
읽지 않는다. “사용할 자료구조”, “상태 정의”, “정렬 기준” 중 하나만 확인하고
다시 15분 시도한다.

### B. 통과 직후: 설명 없는 통과를 금지

아래 다섯 줄을 `REVIEW.md`나 개인 오답 노트에 남긴다.
[훈련 기록 템플릿](./STUDY_LOG_TEMPLATE.md)을 복사해 사용해도 된다.

```text
분류 근거:
핵심 불변식/점화식:
시간·공간복잡도:
내가 놓친 반례:
다시 풀 날짜: D+1 / D+7 / D+30
```

### C. 재풀이: 코드를 외우지 말고 결정을 복원

- D+1: 빈 파일에서 20분 안에 다시 푼다.
- D+7: 유형 이름을 가리고 15~25분 안에 다시 푼다.
- D+30: 비슷한 새 문제와 섞어 실전 시간으로 푼다.

같은 코드를 기억해 쓰는 것은 숙련이 아니다. 왜 이 자료구조를 골랐고 다른
접근이 왜 느리거나 틀리는지 설명해야 한다.

## 3. 성장 단계와 통과 기준

### 단계 0 — 문법 안정화

- 리스트·딕셔너리·집합·정렬·`deque`·`heapq`를 문서 없이 사용한다.
- 인덱스, 슬라이싱, 정수 나눗셈, 문자열 변환에서 실수가 없다.
- 목표: Level 1을 20분 내 안정적으로 통과한다.

### 단계 1 — 패턴 인식

- 문제를 읽고 3분 안에 후보 유형을 두 개 이하로 줄인다.
- 해시, 정렬, 스택/큐, 완전탐색, 기본 BFS를 구현한다.
- 목표: 이 저장소 Level 1 전부 + Kit Level 2의 70%를 도움 없이 해결한다.

### 단계 2 — 증명과 최적화

- 그리디 교환 논증, DP 상태 정의, 단조성 기반 이분탐색을 설명한다.
- 최악 입력에서 시간과 메모리를 계산한다.
- 목표: Level 2를 평균 30분, Level 3을 평균 50분 내 해결한다.

### 단계 3 — 혼합 유형

- 정렬+그리디, BFS+상태압축, 이분탐색+그리디 판정처럼 두 기법을 결합한다.
- 구현이 긴 문제에서도 함수 분리와 검증 순서를 유지한다.
- 목표: 2시간 모의고사에서 쉬운 문제를 모두 확보하고 중상 난도 한 문제 이상을 푼다.

### 단계 4 — 대회 운영

- 10분 안에 전 문제를 스캔하고 기대 점수 순으로 배치한다.
- 막힌 문제를 중단하고 돌아오는 기준이 있다.
- 제출 전 수동 테스트와 복잡도 검사를 5분 안에 끝낸다.

## 4. 12주 훈련 계획

하루 2시간, 주 6일 기준이다. 시간이 절반이면 기간을 두 배로 늘리고 복습은
줄이지 않는다.

| 주차 | 핵심 | 이 저장소 사용법 | 추가 훈련 |
|---:|---|---|---|
| 1 | Python·복잡도·해시 | `01_해시` 전부 | 빈도, 매핑, 중복 처리 5문제 |
| 2 | 스택/큐·정렬 | `02`, `04` | 단조 스택, 사용자 정렬 키 5문제 |
| 3 | 완전탐색 | `05` | 조합·순열·백트래킹 변형 6문제 |
| 4 | BFS/DFS | `08`의 Level 2 중심 | 격자·연결요소·최단거리 8문제 |
| 5 | 힙·그리디 | `03`, `06` | 스케줄링·구간 문제 6문제 |
| 6 | 이분탐색 | `09` | 답 이분탐색 6문제 |
| 7 | DP 기초 | `07` 앞 3문제 분석 | 1차원·격자 DP 8문제 |
| 8 | 그래프 | `10`, `06/섬 연결하기` | 최단거리·MST·위상정렬 입문 |
| 9 | 혼합 유형 | 미해결 Kit 문제 | Level 2/3 랜덤 12문제 |
| 10 | 구현·시뮬레이션 강화 | 긴 설명 문제 재풀이 | 90~120분 모의고사 3회 |
| 11 | 약점 보완 | 취약 유형 집중 | 2시간 모의고사 4회 |
| 12 | 실전 안정화 | 오답만 재풀이 | 실제 환경 리허설 3회 |

### 주간 리듬

- 월~목: 새 문제 2개 + 이전 문제 1개 재풀이
- 금: 오답만 재풀이하고 유형 가이드를 한 번 갱신
- 토: 시간 제한 모의고사와 2시간 복기
- 일: 휴식 또는 30분 플래시카드 복습

## 5. 정량 지표

문제 수보다 다음 값을 기록한다.

| 지표 | 초급 목표 | 합격권 목표 | 마스터 목표 |
|---|---:|---:|---:|
| 유형 판별 시간 | 10분 | 5분 | 2~3분 |
| Level 1 풀이 | 30분 | 15~20분 | 10분 내 |
| Level 2 풀이 | 60분 | 30~40분 | 20~30분 |
| 첫 제출 정답률 | 40% | 70% | 85% 이상 |
| D+7 무힌트 재현율 | 50% | 80% | 95% 이상 |
| 시간복잡도 설명 | 대략적 | 정확한 Big-O | 병목 연산까지 설명 |

첫 제출 정답률이 낮으면 더 많은 문제를 풀기보다 반례 설계와 수동 추적을 늘린다.

## 6. Python 실전 도구 상자

외우는 대상은 긴 정답 코드가 아니라 다음 도구의 성질이다.

```python
from collections import Counter, defaultdict, deque
from itertools import combinations, permutations, product
from bisect import bisect_left, bisect_right
import heapq
```

- `dict`, `set`: 평균 `O(1)` 조회·삽입, 키는 해시 가능해야 한다.
- `deque`: 양끝 삽입·삭제 `O(1)`. 리스트의 `pop(0)`은 피한다.
- `heapq`: 최소 힙. 최대 힙은 음수 변환이나 정렬 기준 튜플을 쓴다.
- `sort`: `O(n log n)`, 안정 정렬. 다중 기준은 튜플 키로 표현한다.
- 재귀 DFS: 깊이가 클 수 있으면 반복형 또는 재귀 한도와 스택 위험을 검토한다.
- 큰 정수: Python은 오버플로가 없지만 불필요한 큰 수 연산은 느릴 수 있다.

## 7. 모의고사 운영법

저장소에서 바로 시작하려면 `python3 tools/mock.py start`를 실행한다. 기본값은
120분에 Level 1 한 문제와 Level 2 두 문제이며, 서로 다른 유형을 우선 선택한다.
`status`로 남은 시간을 확인하고 `finish`로 세 문제의 공개·경계 테스트를 채점한다.
세션 결과는 `.practice/`에 남으므로 아래 복기 양식과 함께 비교한다.

### 시작 10분

모든 문제를 읽고 다음 표만 만든다.

```text
문제 | 예상 유형 | 예상 복잡도 | 구현량 | 확신도 | 순서
```

“쉬워 보인다”가 아니라 점수/시간 비율로 순서를 정한다. 일반적으로 즉시 구현 가능한
문제 → 확실한 중간 문제 → 탐색이 필요한 문제 순이다.

### 중단 규칙

- 15분 동안 상태 정의가 안 되면 다음 문제로 이동한다.
- 구현 후 15분 동안 같은 실패를 반복하면 최소 반례를 저장하고 이동한다.
- 남은 시간이 30분이면 새 어려운 문제보다 기존 코드 검증을 우선한다.

### 제출 전 5분 체크

- 빈 입력 또는 최소 입력을 처리했는가?
- 정렬 전후 원본 인덱스 의미가 유지되는가?
- 중복 원소를 값이 아니라 개수로 처리해야 하는가?
- `<=`와 `<`, 0/1 기반 인덱스가 맞는가?
- 입력 배열을 수정해 다음 테스트에 영향을 주지 않는가?
- 최악 입력에서 반복문이 몇 번 도는가?
- 반환 타입과 순서가 정확한가?

## 8. 유형별 가이드

| 유형 | 가이드 | 핵심 질문 |
|---|---|---|
| 해시 | [01_해시/MASTERY_GUIDE.md](./01_해시/MASTERY_GUIDE.md) | 무엇을 키로 저장하면 반복 탐색을 없앨까? |
| 스택/큐 | [02_스택_큐/MASTERY_GUIDE.md](./02_스택_큐/MASTERY_GUIDE.md) | 다음에 처리할 원소의 순서는 무엇인가? |
| 힙 | [03_힙/MASTERY_GUIDE.md](./03_힙/MASTERY_GUIDE.md) | 계속 필요한 극값을 어떻게 유지할까? |
| 정렬 | [04_정렬/MASTERY_GUIDE.md](./04_정렬/MASTERY_GUIDE.md) | 어떤 순서로 놓으면 관계가 단순해질까? |
| 완전탐색 | [05_완전탐색/MASTERY_GUIDE.md](./05_완전탐색/MASTERY_GUIDE.md) | 후보 공간을 빠짐없이 어떻게 생성할까? |
| 탐욕법 | [06_탐욕법/MASTERY_GUIDE.md](./06_탐욕법/MASTERY_GUIDE.md) | 지금의 선택이 미래를 해치지 않음을 어떻게 증명할까? |
| 동적계획법 | [07_동적계획법/MASTERY_GUIDE.md](./07_동적계획법/MASTERY_GUIDE.md) | 같은 부분 문제를 어떤 상태로 합칠까? |
| DFS/BFS | [08_DFS_BFS/MASTERY_GUIDE.md](./08_DFS_BFS/MASTERY_GUIDE.md) | 정점, 간선, 방문 상태는 무엇인가? |
| 이분탐색 | [09_이분탐색/MASTERY_GUIDE.md](./09_이분탐색/MASTERY_GUIDE.md) | 답 후보에 참/거짓 경계가 존재하는가? |
| 그래프 | [10_그래프/MASTERY_GUIDE.md](./10_그래프/MASTERY_GUIDE.md) | 관계를 어떤 그래프 문제로 번역할까? |

## 9. 최종 마스터 체크리스트

- [ ] 10개 유형을 정의가 아니라 문제 신호와 불변식으로 설명할 수 있다.
- [ ] 모든 Kit 문제를 D+30에 무힌트로 다시 풀 수 있다.
- [ ] 각 풀이의 시간·공간복잡도와 병목을 말할 수 있다.
- [ ] 느린 기준 풀이와 빠른 풀이를 작은 랜덤 입력으로 비교할 수 있다.
- [ ] 2시간 모의고사 5회 연속으로 쉬운 문제를 모두 맞힌다.
- [ ] 첫 제출 정답률이 80% 이상이다.
- [ ] 실패 원인을 유형 미인지/증명 오류/구현 오류/복잡도 오류로 분류한다.

마스터는 모든 문제를 본 사람이 아니라, 낯선 문제에서도 같은 사고 절차를 재현하는
사람이다. 속도는 이 절차를 생략해서가 아니라 자동화해서 얻는다.
