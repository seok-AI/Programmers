# 정수 삼각형 — 예시 풀이

정답을 보기 전에 자신의 풀이를 완성하고, D+1 재풀이까지 끝낸 뒤 비교하는 것을
권장합니다. 아래 코드는 인터넷 코드를 그대로 복제하지 않고 공개적으로 널리 쓰이는
접근을 바탕으로 이 저장소를 위해 독립 작성했습니다.

| 파일 | 접근 | 복잡도 | 용도 |
|---|---|---|---|
| [효율적인 권장 풀이](./recommended.py) | 아래 행에서 위로 올라오며 최선의 자식 합 선택 | `O(n^2) time, O(n^2) space` | 실전 제출의 기준 풀이 |
| [공개 풀이 접근(재작성)](./alternative.py) | 꼭대기에서 각 칸까지 최대 합을 한 행씩 갱신 | `O(n^2) time, O(n) space` | 같은 문제를 다른 상태 표현으로 비교하는 풀이 |

## 풀이별 해설

### `recommended.py` — 효율적인 권장 풀이

- 핵심 사고: 아래 행에서 위로 올라오며 최선의 자식 합 선택
- 복잡도: `O(n^2) time, O(n^2) space`
- 용도: 문제 제한을 먼저 계산한 뒤 실전 제출 답안의 기준으로 삼습니다.
- 읽는 법: 코드 주석에서 핵심 상태와 목표 복잡도를 확인하고, `HINTS.md`의 마지막
  단계와 자신의 구현 순서를 비교하세요.

### `alternative.py` — 공개 풀이 접근을 주석과 함께 재작성

- 핵심 사고: 꼭대기에서 각 칸까지 최대 합을 한 행씩 갱신
- 복잡도: `O(n^2) time, O(n) space`
- 적용 범위: 문제의 최대 제한에서도 실행 가능한 대안
- 문제별 주의: 위에서 내려오는 DP와 아래에서 올라오는 DP 모두 가능하지만 인접 가능한 두 칸의 인덱스를 정확히 잡는다.
- 출처 정책: [SOURCES.md](./SOURCES.md)에 정확한 스냅숏과 재작성 범위를 기록했습니다.

## 비교 방법

1. 두 파일에서 상태 정의와 핵심 불변식을 찾습니다.
2. 같은 입력을 어떤 순서로 처리하는지 손으로 추적합니다.
3. 최대 제한에서 시간·공간 차이를 계산합니다.
4. 대안 풀이가 느리다고 표시된 경우 작은 입력의 정답 오라클로만 사용합니다.

## 인터넷 참고

- [프로그래머스 문제별 풀이 Q&A](https://school.programmers.co.kr/learn/courses/30/lessons/43105/questions)
- [공개 풀이 모음 저장소](https://github.com/codeisneverodd/programmers-coding-test)

참고 링크의 코드는 작성자별 스타일과 복잡도가 다릅니다. 정답 여부만 믿지 말고 현재
제한에서의 복잡도와 경계 처리를 직접 검증하세요.
