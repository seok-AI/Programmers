# 테스트 전략과 통과 판정의 의미

## 결론부터

이 저장소에서 `통과`는 다음 세 층을 모두 만족했다는 뜻이다.

1. 공개 예제와 사람이 검토한 경계·함정 사례
2. 명세 안의 작은 입력을 대량 생성한 결정적 랜덤 교차검증
3. 최대 입력 또는 시간복잡도에 불리한 적대 입력 성능검사

단, 테스트는 허용 입력 전체에 대한 수학적 증명이 아니다. 예를 들어 길이 100,000의
정수 배열만 해도 가능한 배열 수가 사실상 무한에 가깝다. 따라서 테스트 통과 뒤에도
불변식·정당성 증명과 공식 채점이 필요하다.

## 1. 고정 회귀 테스트

- `tests.json`: 공식 공개 예제를 출처와 함께 보존
- `edge_tests.json`: 최소값, 동률, 중복, 불가능, 경계 인덱스, 특수 반환값
- 현재 총계: 공개 114개 + 경계 114개 = 228개
- `tools/input_constraints.py`가 57문제의 기계화 가능한 길이·범위·형식·그래프 연결성
  제약을 검사하고, `tools/validate.py`가 공개·경계 사례 전부에 이를 적용

고정 테스트의 장점은 실패가 재현되고 디버깅하기 쉽다는 것이다. 단점은 네 사례만
외워서 통과하는 잘못된 구현을 잡기 어렵다는 것이다.

## 2. 결정적 랜덤 교차검증

[`tools/fuzz_case_bank.py`](./tools/fuzz_case_bank.py)는 기본 57문제 모두에 명세 안의
작은 입력 생성기를 제공한다. 각 입력의 기대값은 다음 순서로 정한다.

1. 입력을 생성하고 같은 문제의 로컬 제한사항을 지키는지 검사한다.
2. `solutions/recommended.py`를 깊은 복사 입력으로 실행한다.
3. 별도로 작성된 `solutions/alternative.py`를 같은 입력으로 실행한다.
4. 두 결과가 완전히 같을 때만 기대값으로 채택한다.
5. 두 풀이가 다르면 사용자 코드를 판정하지 않고 오라클 오류로 중단한다.
6. 사용자 `solution()`을 다시 깊은 복사 입력으로 실행해 기대값과 비교한다.

기본값은 문제당 500개다. 57문제를 모두 풀었다면 한 번에 28,500개의 사용자 입력을
검사한다. 랜덤처럼 보이지만 기준 시드가 고정되어 같은 명령은 같은 실패를 재현한다.

```bash
# 한 문제: 사용자 풀이에 랜덤 500개
python3 tools/fuzz.py 42746

# 더 강하게: 같은 문제 10,000개
python3 tools/fuzz.py 42746 --cases 10000 --seed 7

# 예시 풀이 두 개와 57개 생성기의 합의만 검사: 28,500개
python3 tools/fuzz.py --all --oracles-only

# 다른 입력군을 다시 검사
python3 tools/fuzz.py 42746 --cases 5000 --seed 20260804
```

시드와 사례 번호가 실패 출력에 표시되므로, 같은 명령으로 재현한 뒤 해당 `args`를
`edge_tests.json`에 영구 회귀 사례로 옮긴다.

## 3. 최대·적대 입력 성능검사

`tools/stress.py`는 작은 랜덤 입력이 발견하기 어려운 시간복잡도 퇴행을 검사한다.
현재 22문제의 권장·대안 풀이 44개가 등록되어 있다.

```bash
python3 tools/stress.py --all
python3 tools/stress.py 42626 --timeout 3
```

랜덤 테스트는 주로 정확성, 스트레스 테스트는 실행 가능성을 판정한다. 작은 입력
10,000개 통과는 `O(n²)` 풀이가 길이 100,000에서 끝난다는 보장이 아니다.

## `solution.py`를 직접 실행할 때

각 문제의 `solution.py`를 실행하면 다음이 자동 수행된다.

1. 공개·고정 경계 사례의 입력, 기대값, 실제값, 실행시간을 모두 출력한다.
2. 이어서 랜덤 500개를 실행한다.
3. 랜덤 통과 사례는 요약하고, 실패 사례는 최대 5개까지 실제 `args`와 결과를 출력한다.
4. 고정 실패, 랜덤 실패, 예시 풀이 불일치 중 하나라도 있으면 종료 코드 1을 반환한다.

```bash
python3 "04_정렬/04_42746_가장_큰_수/solution.py"
```

## 알려진 한계

- 권장·대안 풀이가 같은 논리 오류를 공유하면 교차검증도 놓칠 수 있다.
- 랜덤 분포는 가능한 입력을 균등하게 전부 열거하지 않는다.
- `아이템 줍기`의 랜덤 생성은 단일 직사각형을 집중 검사하며, 복합 직사각형은 공개·고정
  사례가 담당한다.
- `퍼즐 조각 채우기` 랜덤 생성은 한 빈 영역과 한 조각의 회전·불일치에 집중하고,
  여러 컴포넌트의 개수 소비는 공개·고정 사례가 담당한다.
- 현재 최대 입력 성능검사는 22/57문제다. 나머지 문제도 입력 상한이 크거나 구현이
  바뀌면 `stress.py`에 추가해야 한다.

그러므로 가장 강한 판정은 `고정 + 여러 시드의 랜덤 + 스트레스 + 불변식 설명 + 공식
채점`의 조합이다. 로컬 테스트 결과만으로 프로그래머스 비공개 테스트 전체 통과를
보증한다고 표현하지 않는다.
